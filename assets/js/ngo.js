document.addEventListener("ngo:data-ready",()=>{const m=document.querySelector("#ngo-profile");if(!m)return;const id=new URLSearchParams(location.search).get("id"),n=NGOEffects.DATA.ngos.find(x=>x.id===id);if(!n){m.innerHTML=`<div class="empty-state"><span class="eyebrow">404 · PROFILE</span><h1>NGO profile not found.</h1><p>The requested organisation is not present in the current curated website-backed directory.</p><a class="action-dark" href="ngos.html">Back to Explore NGOs</a></div>`;return}document.title=`${n.name} | NGOEffects`;m.innerHTML=`<section class="profile-header"><div class="profile-topline"><span class="eyebrow">WEBSITE-BACKED NGO PROFILE</span><span>${NGOEffects.esc(n.state)}</span></div><h1>${NGOEffects.esc(n.name)}</h1><p>${NGOEffects.esc(n.description)}</p><img class="profile-photo" src="${NGOEffects.imageFor(n)}" alt="Open-source community work image"><div class="profile-meta"><span>HQ / listed state: ${NGOEffects.esc(n.state)}</span><span>Districts: ${NGOEffects.esc((n.districts||[]).join(" · ")||"State / regional")}</span></div></section><section class="profile-body"><div><span class="eyebrow">Sectors</span><div class="tags profile-tags">${(n.sectors||[]).map(x=>`<span>${NGOEffects.esc(x)}</span>`).join("")}</div><span class="eyebrow">Published operating areas</span><div class="state-pills">${(n.operatingStates||[]).map(x=>`<span>${NGOEffects.esc(x)}</span>`).join("")}</div></div><div class="profile-actions"><a class="action-dark" href="${NGOEffects.esc(n.website)}" target="_blank" rel="noopener noreferrer">Visit official website ↗</a>${n.contactPage?`<a class="action-outline" href="${NGOEffects.esc(n.contactPage)}" target="_blank" rel="noopener noreferrer">Contact page ↗</a>`:""}</div></section>`});